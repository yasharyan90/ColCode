# Copy to .env — every service reads it via `npm run dev` (dotenv is loaded by tsx --env-file)
DATABASE_URL=postgres://colcode:colcode@localhost:5432/colcode
REDIS_URL=redis://localhost:6379
JWT_SECRET=change-me-to-a-long-random-string
# GitHub OAuth app (Settings → Developer settings → OAuth Apps). Callback must match exactly.
GITHUB_CLIENT_ID=Ov23liXxvONYQJcr8RDY
GITHUB_CLIENT_SECRET=644451c89eac76a317ef6304c2febb2947c02152
OAUTH_CALLBACK_URL=http://localhost:5173/api/auth/github/callback
# Dev-only password-less login (never enabled when NODE_ENV=production)
AUTH_DEV_LOGIN=1
APP_URL=http://localhost:5173
